import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 12,
    backgroundColor: '#fff',
  },
  infoContainer: {
    padding: 12,
  },
  imageContainer: {
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  image: {
    width: '100%',
    aspectRatio: 16 / 9,
  },
  title: {
    fontSize: 16,
    color: '#000',
    marginBottom: 12,
    fontWeight: 'bold',
  },
  info: {
    color: '#000',
  },
});
