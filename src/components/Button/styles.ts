import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#6177f6ff',
    width: '100%',
    padding: 12,
    alignItems: 'center',
  },
  buttonText: {
    color: '#fff',
  },
  disabled: {
    opacity: 0.7,
  },
});
